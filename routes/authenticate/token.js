const app = require('express')();
const debug = require('debug')('development');
const jwt = require('jsonwebtoken');

app.route('/')
	.get((req, res, next) => {

	})
	.post((req, res, next) => {

	});

module.exports = app;
